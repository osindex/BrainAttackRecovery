import type { RouteRecordRaw } from 'vue-router';

// Content-management pages are delivered by source plugins through backend
// menus and the plugin page host.
const routes: RouteRecordRaw[] = [];

export default routes;
