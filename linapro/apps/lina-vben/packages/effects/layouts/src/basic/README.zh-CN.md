# basic layout

## Header Slots

- `header-left-n`：排序区间为 `0-19`，随后 `breadcrumb` 从 `21+` 开始
- `header-right-n`：排序区间为 `0-49`，之后依次是 `global-search`、`theme-toggle`、`language-toggle`、`fullscreen`、`notification`、`user-dropdown`，以及 `151+` 之后的自定义插槽
