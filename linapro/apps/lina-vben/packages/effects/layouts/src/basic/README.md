# basic layout

## Header Slots

- `header-left-n`: sorted in the `0-19` range, then `breadcrumb` from `21+`
- `header-right-n`: sorted in the `0-49` range, then `global-search`, `theme-toggle`, `language-toggle`, `fullscreen`, `notification`, `user-dropdown`, and custom slots after `151+`
