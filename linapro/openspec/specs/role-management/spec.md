# 角色管理

## 目的

定义角色查询、维护、权限分配、状态控制、用户分配、插件菜单授权、本地化种子展示行为、批量删除和分布式权限缓存失效。

## 需求

### 需求：角色列表和详情查询

系统 SHALL 提供分页角色列表和详情 API，返回角色标识、权限键、排序、数据范围、状态、备注、时间戳以及适用时的已分配菜单 ID。

#### 场景：查询角色列表

- **当** 用户打开角色管理时
- **则** 系统返回分页角色
- **且** 支持按角色名称、权限键和状态筛选

#### 场景：查询角色详情

- **当** 用户打开现有角色进行编辑时
- **则** 系统返回角色详情和已分配的菜单 ID

### 需求：角色创建和更新操作

系统 SHALL 支持创建和更新角色，同时强制角色名称和权限键唯一。

#### 场景：创建角色

- **当** 用户提交有效的角色名称、权限键、排序、数据范围、状态和备注时
- **则** 系统创建角色并记录时间戳

#### 场景：拒绝重复的角色标识

- **当** 提交的角色名称或权限键已存在时
- **则** 系统返回验证或业务错误

#### 场景：更新角色菜单权限

- **当** 用户更新角色的菜单分配时
- **则** 旧的 `sys_role_menu` 记录被新选择替换
- **且** 父子菜单分配语义被保留

### 需求：角色删除和状态控制

系统 SHALL 支持删除角色和切换启用或禁用状态，同时保留受保护的内置角色规则。删除必须通过事务原子执行，事务内任何关联记录删除失败必须回滚整个操作。服务不得仅记录警告并继续删除角色本身。访问拓扑变更通知必须在事务提交后发出。

#### 场景：删除未分配给用户的角色

- **当** 用户删除未分配给用户的角色时
- **则** 系统在事务内通过设置 `deleted_at` 软删除该角色
- **且** 同步删除 `sys_role_menu` 和 `sys_user_role` 中的相关记录
- **且** 仅在事务提交后通知访问拓扑变更

#### 场景：删除已分配给用户的角色

- **当** 用户尝试删除已分配给用户的角色时
- **则** 系统要求确认该角色已分配并将从这些用户中移除
- **且** 确认后，系统在事务内删除该角色并取消这些角色分配

#### 场景：受保护的管理员角色不能被删除

- **当** 用户尝试删除内置管理员角色时
- **则** 系统拒绝删除

#### 场景：关联清理失败回滚整个操作

- **当** 用户删除角色
- **且** `sys_role_menu` 或 `sys_user_role` 的清理在事务内返回错误时
- **则** 角色软删除也必须回滚
- **且** 操作返回底层错误而非仅记录警告
- **且** 不发出访问拓扑变更通知

#### 场景：禁用角色

- **当** 角色状态变为禁用时
- **则** 与该角色关联的用户登录后不再获得该角色的菜单权限

### 需求：角色选项和用户分配能力

系统 SHALL 为用户管理和角色用户分配工作流提供角色选项 API。批量授予和撤销操作必须在一个事务内提交。所有插入或删除操作必须一起成功或一起回滚。系统不得在记录失败的逐行插入警告后继续处理后续项。

#### 场景：获取角色选项

- **当** 用户表单加载时
- **则** 系统返回包含 `id`、`name` 和 `key` 的启用角色选项

#### 场景：查看分配给角色的用户

- **当** 用户点击角色分配时
- **则** 系统显示已分配用户的分页和搜索

#### 场景：撤销用户的角色授权

- **当** 用户在角色用户列表中点击撤销授权时
- **则** 系统在事务内删除对应的 `sys_user_role` 记录
- **且** 用户列表自动刷新

#### 场景：批量授权原子提交

- **当** 用户选择多个未分配该角色的用户并点击授权时
- **则** 系统在一个事务内批量插入 `sys_user_role` 关联记录
- **且** 任何插入失败必须回滚整个操作
- **且** 这些用户再次登录后可获得该角色的菜单权限

### 需求：必须支持数据范围选项

系统 SHALL 支持全部数据、本部门数据和本人创建数据的简化数据范围值。角色数据范围 SHALL 被宿主数据权限治理能力用作当前用户有效数据范围解析的输入，而不只是角色记录上的展示字段。角色数据范围创建、更新、启用、禁用、角色菜单权限变更以及用户角色关系变更后，系统 SHALL 触发访问拓扑或数据权限缓存失效，使后续受保护 API 不会无限期使用旧的数据范围。组织管理能力未启用时，角色新增和编辑表单 SHALL 隐藏或禁用本部门数据选项，避免配置无法生效的数据范围。

#### 场景：配置数据范围

- **当** 角色数据范围设置为支持的值之一时
- **则** 系统存储该值供宿主数据权限治理使用

#### 场景：角色数据范围参与数据权限解析

- **当** 用户拥有启用角色且该角色 `dataScope=2` 时
- **则** 宿主数据权限治理能力将该角色视为本部门数据范围输入
- **且** 接入数据权限治理的宿主资源按本部门范围限制数据

#### 场景：组织能力未启用时隐藏本部门数据选项

- **当** `org-center` 未安装、未启用或组织能力 provider 不可用时
- **且** 管理员打开角色新增或编辑表单
- **则** 数据权限选项中不允许选择本部门数据范围
- **且** 新建角色默认使用全部数据或仅本人数据中的可用选项

#### 场景：角色数据范围变更触发缓存失效

- **当** 管理员修改角色的 `dataScope` 并保存成功时
- **则** 系统发布访问拓扑或数据权限缓存修订号
- **且** 拥有该角色的用户后续请求不得无限期沿用修改前的数据范围

#### 场景：禁用角色不再贡献数据范围

- **当** 管理员禁用某个角色时
- **则** 与该角色关联的用户登录后不再获得该角色的菜单权限
- **且** 宿主数据权限治理能力不再使用该角色的 `dataScope`

### 需求：角色必须支持插件菜单和权限授权

系统 SHALL 允许管理员在角色授权流程中分配插件菜单和插件按钮权限，并 SHALL 在插件禁用时保留授权关系。角色新增和编辑抽屉使用的菜单权限树 SHALL 以授权展示结构为准，而不是直接暴露原始 `sys_menu` 父子结构中的所有边界情况：所有菜单权限节点必须位于目录节点之下，所有按钮权限节点必须位于菜单节点之下。动态插件或运行时同步产生的权限若缺少合适的目录或菜单父级，系统 SHALL 在权限树投影中归入专用动态权限分组；该分组仅用于授权展示，不得作为真实业务菜单写回 `sys_menu`。动态权限分组 SHALL 排在宿主稳定系统目录之后，避免出现在权限树最前面。

#### 场景：为角色分配插件菜单

- **当** 管理员打开角色菜单授权树且插件已安装并启用时
- **则** 插件菜单和按钮权限出现在可分配树中

#### 场景：插件禁用保留角色授权记录

- **当** 有现有角色授权的插件被禁用时
- **则** 这些角色暂时停止接收该插件的菜单和权限
- **且** 系统保留原始授权记录供后续恢复

#### 场景：动态权限按授权展示层级归组

- **当** 角色新增或编辑抽屉加载菜单权限树
- **且** 存在根级菜单权限、根级按钮权限或动态路由生成的按钮权限缺少菜单父级时
- **则** 返回给角色抽屉的权限树将这些节点归入专用动态权限目录
- **且** 根级菜单权限在该目录下作为菜单节点展示
- **且** 根级按钮权限在该目录下的专用权限菜单节点下展示
- **且** 该整理只影响权限树响应，不修改 `sys_menu.parent_id`

#### 场景：动态权限排在系统菜单之后

- **当** 角色新增或编辑抽屉加载菜单权限树
- **且** 权限树中同时存在宿主稳定目录和动态权限分组
- **则** 动态权限分组出现在宿主稳定目录之后
- **且** 不因动态插件菜单 `sort` 值较小而出现在权限树最前面

### 需求：内置角色展示必须本地化一致

内置受保护角色和默认种子角色 SHALL 在框架交付页面中按当前语言显示。用户管理和角色管理必须对同一内置角色显示相同的本地化显示值。

#### 场景：默认用户角色以英文显示

- **当** 管理员以 `en-US` 打开角色管理时
- **则** 默认用户角色显示英文名称
- **且** 不保持为原始中文种子值

#### 场景：用户管理角色显示与角色管理匹配

- **当** 管理员以 `en-US` 打开用户管理时
- **则** 管理员用户的角色名称与英文角色管理显示匹配
- **且** 前端不维护基于中文名称或角色键的额外映射

### 需求：角色批量删除

系统 SHALL 提供 `DELETE /api/v1/role?ids=...` 用于批量删除角色，复用所有单条删除保护规则，并保证整个操作的事务原子性。

#### 场景：删除多个未分配给用户的角色

- **当** 拥有 `system:role:remove` 权限的调用方调用 `DELETE /api/v1/role?ids=2,3,4`
- **且** 目标角色均未分配给用户且均非超级管理员角色
- **则** 系统在一个事务内软删除所有角色
- **且** 同步删除相关的 `sys_role_menu` 和 `sys_user_role` 关联记录
- **且** 提交后触发一次访问拓扑变更通知

#### 场景：包含超级管理员角色的批量被拒绝

- **当** 调用方调用 `DELETE /api/v1/role?ids=1&ids=2&ids=3`
- **且** id `1` 是超级管理员角色
- **则** 整个批量必须被拒绝
- **且** 不删除任何角色

#### 场景：空 id 列表在验证时被拒绝

- **当** 调用方调用 `DELETE /api/v1/role?ids=` 时
- **则** 系统返回参数验证错误
- **且** 不启动事务

### 需求：权限拓扑缓存必须可靠地跨节点失效

角色、菜单、用户角色、插件权限菜单或权限资源关系变更后，系统 SHALL 通过统一缓存协调机制可靠地使所有节点上的 Token 权限快照失效。

#### 场景：角色菜单权限变更

- **当** 管理员更新角色菜单或按钮权限时
- **则** 系统提交角色权限关系变更
- **且** 可靠发布权限拓扑缓存修订号
- **且** 所有节点在权限缓存域允许的陈旧窗口内丢弃旧的 Token 权限快照

#### 场景：菜单权限标识变更

- **当** 管理员创建、更新、删除或禁用菜单权限时
- **则** 系统发布权限拓扑缓存修订号
- **且** 后续受保护 API 权限检查不得无限期使用旧的菜单权限拓扑

#### 场景：插件权限拓扑变更

- **当** 插件安装、启用、禁用、卸载或同步变更插件菜单或按钮权限时
- **则** 系统发布权限拓扑缓存修订号
- **且** 受影响的权限根据最新插件状态在所有节点上参与授权决策

### 需求：权限拓扑失效发布失败不得静默成功

关键权限拓扑写路径在无法发布权限缓存修订号时必须返回结构化错误或回滚业务变更。这防止集群节点继续使用旧的授权快照。

#### 场景：权限修订号发布失败

- **当** 角色、菜单或用户角色写路径需要发布权限拓扑修订号但发布失败时
- **则** 系统返回结构化业务错误
- **且** 调用方不得收到声称权限变更已完全生效的成功响应
- **且** 系统记录失败原因供重试或修复

#### 场景：受保护 API 看到过期的权限缓存

- **当** 受保护 API 验证权限、无法确认本地权限快照新鲜度且超过故障窗口时
- **则** 系统按失败关闭策略拒绝请求
- **且** 系统不得因旧的本地权限快照而继续允许不确定的权限
