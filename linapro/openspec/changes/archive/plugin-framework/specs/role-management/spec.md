## Requirements

### Requirement: Roles Support Plugin Menu and Permission Authorization

The system SHALL allow administrators to assign plugin menu and button permissions in role authorization flows.

### Requirement: Plugin Disable Preserves Role Authorization

The system SHALL preserve role-to-plugin-menu authorization when a plugin is disabled, restoring on re-enable.
